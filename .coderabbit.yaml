# .coderabbit.yaml
reviews:
  # Never auto-review anything
  auto_review:
    enabled: false
    drafts: false

  # Never touch / rewrite the PR description
  high_level_summary: false

  # Keep the rest quiet
  poem: false
  sequence_diagrams: false
  review_status: false
